# Hoist all packages to root node_modules so Expo/Metro can resolve them.
# React Native's Metro bundler does not follow pnpm's default symlink structure.
shamefully-hoist=true
strict-peer-dependencies=false
