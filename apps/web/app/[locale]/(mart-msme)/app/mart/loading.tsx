import { CartSkeleton } from '@/components/mart/skeletons'

/** Covers /app/mart/cart and /app/mart/checkout — both are a seller sheet + emerald totals + sticky action. */
export default function Loading() {
  return <CartSkeleton />
}
